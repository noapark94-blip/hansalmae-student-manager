Run `node --experimental-strip-types --test tests/settings-concurrency/helpers.test.mjs` for normalization, grouped fields and comparison choices. Execute database-rollback.sql inside BEGIN/ROLLBACK after the migration: calendar/class disjoint edits merge, stale same-field changes return conflicts, stale deletion is rejected, class schedule IDs and explicit student memberships remain unchanged, anonymous saves fail.

The academic calendar and class manager use changed-field RPCs with row locks and the existing comparison dialog. Calendar timing and scope/category are coherent groups; class schedule and teacher lists are grouped. Inputs remain intact on cancel/error. The separate class timetable editor checks its original schedule/teacher snapshot and stops stale saves/deletes; new schedules read the initial teacher list before editing.

Class settings keep matching schedule IDs instead of deleting every schedule. Existing historical schedules are retained. Removing/replacing a currently explicitly assigned slot is blocked with an instruction to adjust student selections first; the individual timetable editor can change an existing slot in place. No student subset is silently widened to every class time.

Production rollback assertions and Next webpack build passed. Tests model stale editors sequentially, not two independent browser sessions. Two-device/mobile visual verification remains unavailable in this environment. No realtime expansion in this change; only the correction timetable currently has the previously added batched live updates. Legacy RPCs and direct writes remain callable by old clients, so reload after deployment. Unrelated vehicle, enrollment and category editors are outside this change.
