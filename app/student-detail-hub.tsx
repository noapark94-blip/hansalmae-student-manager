"use client";

import type { FormEvent } from "react";
import { useEffect, useState } from "react";
import type { SupabaseClient } from "@supabase/supabase-js";
import { StudentLifecyclePanel } from "./student-lifecycle-panel";
import { StudentRelevantTimetable, type WeeklyTimetableRow } from "./weekly-timetable";

type StudentValues={ id:string; name:string; school:string; grade:string; phone:string; status:string; internalNote:string };
type RosterStudent={ id:string; name:string; school:string|null; grade:string|null; status:string; enrollments:unknown[] };
type Summary={ attendanceTotal:number; present:number; late:number; absent:number; assignmentOpen:number; upcomingMakeups:number; lastConsultedAt:string|null };
type ClassItem={ id:string; name:string; subject:string; room:string|null; status:string; startedOn:string; teachers:string; color?:string };
type AttendanceItem={ id:string; lessonDate:string; className:string; status:"present"|"late"|"absent"|"excused"; note:string|null };
type MakeupItem={ id:string; className:string; missedDate:string; scheduledAt:string; endsAt:string; room:string; status:string; teacherName:string; note:string|null };
type AssignmentItem={ id:string; title:string; className:string; dueAt:string; status:string; feedback:string|null };
type ConsultationItem={ id:string; consultedAt:string; type:string; consultantName:string; internalNote:string|null; nextContactOn:string|null };
type Guardian={ id:string; name:string; phone:string; relationship:string|null; isPrimary:boolean };
type DetailData={ summary:Summary; classes:ClassItem[]; guardians:Guardian[]; attendance:AttendanceItem[]; makeups:MakeupItem[]; assignments:AssignmentItem[]; consultations:ConsultationItem[]; timetable:WeeklyTimetableRow[] };
type Tab="summary"|"profile"|"classes"|"attendance"|"learning";
const statusLabels={ present:"출석",late:"지각",absent:"결석",excused:"인정" };
const consultationLabels:Record<string,string>={ student:"학생",guardian:"학부모",phone:"전화",academic:"학습",other:"기타" };

export function StudentDetailHub({ supabase,student,rosterStudent,timetable,onClose,onUpdate,onDelete,onAssign,onLifecycleUpdated }: { supabase:SupabaseClient; student:StudentValues; rosterStudent?:RosterStudent; timetable:WeeklyTimetableRow[]; onClose:()=>void; onUpdate:(values:StudentValues)=>Promise<void>; onDelete:(student:StudentValues)=>Promise<void>; onAssign:(student:RosterStudent)=>void; onLifecycleUpdated:(status:string)=>void }) {
  const [tab,setTab]=useState<Tab>("summary"); const [data,setData]=useState<DetailData|null>(null); const [loading,setLoading]=useState(true); const [loadError,setLoadError]=useState(""); const [values,setValues]=useState(student); const [submitting,setSubmitting]=useState(false); const [error,setError]=useState(""); const [confirmDelete,setConfirmDelete]=useState(false); const [deleteName,setDeleteName]=useState("");
  useEffect(()=>{ let active=true; void supabase.rpc("staff_student_detail_hub",{ p_student_id:student.id }).then(({ data:next,error:detailError })=>{ if(!active)return; if(detailError)setLoadError("학생 통합 기록을 불러오지 못했습니다."); else {const detail=next as Omit<DetailData,"timetable">;const colors=new Map(timetable.map((row)=>[row.className,row.color]));setData({...detail,classes:detail.classes.map((item)=>({...item,color:colors.get(item.name)})),timetable});} setLoading(false); }); return()=>{active=false;}; },[student.id,supabase,timetable]);
  const update=(field:keyof Omit<StudentValues,"id">,value:string)=>setValues((current)=>({...current,[field]:value}));
  const submit=async(event:FormEvent)=>{event.preventDefault();setSubmitting(true);setError("");try{await onUpdate(values);}catch{setError("학생 정보를 수정하지 못했습니다.");setSubmitting(false);}};
  const remove=async()=>{setSubmitting(true);setError("");try{await onDelete(student);}catch{setError("학생을 삭제하지 못했습니다.");setSubmitting(false);}};
  return <div className="modal-backdrop" onMouseDown={(event)=>{if(event.target===event.currentTarget)onClose();}}><section className="student-modal student-hub-modal" role="dialog" aria-modal="true"><header><div><p className="eyebrow">학생 통합 관리</p><h2>{student.name}</h2><span>{student.school || "학교 미입력"}{student.grade ? ` · ${student.grade}`:""} · {studentStatus(student.status)}</span></div><button type="button" aria-label="닫기" onClick={onClose}>×</button></header><nav className="student-hub-tabs">{([['summary','요약'],['profile','기본정보'],['classes','수강 클래스'],['attendance','출결·보강'],['learning','과제·상담']] as [Tab,string][]).map(([id,label])=><button key={id} className={tab===id?"active":""} onClick={()=>setTab(id)}>{label}</button>)}</nav><div className="student-hub-body">{loading&&<p className="hub-message">학생 기록을 불러오는 중이에요…</p>}{loadError&&<p className="hub-message error">{loadError}</p>}{data&&tab==="summary"&&<SummaryTab data={data}/>} {tab==="profile"&&<><form className="student-profile-form" onSubmit={submit}><div className="form-grid"><label>학생 이름 <b>*</b><input required value={values.name} onChange={(event)=>update("name",event.target.value)}/></label><label>학교<input value={values.school} onChange={(event)=>update("school",event.target.value)}/></label><label>학년<input value={values.grade} onChange={(event)=>update("grade",event.target.value)}/></label><label>학생 연락처<input value={values.phone} onChange={(event)=>update("phone",event.target.value)}/></label><label>현재 상태<input disabled value={studentStatus(values.status)}/></label><label className="full">내부 메모<textarea rows={3} value={values.internalNote} onChange={(event)=>update("internalNote",event.target.value)}/></label></div><div className="student-detail-actions"><span>{rosterStudent&&<button type="button" className="secondary-button" onClick={()=>onAssign(rosterStudent)}>＋ 수강 클래스 배정</button>}</span><button type="button" className="danger-link" onClick={()=>setConfirmDelete((current)=>!current)}>학생 삭제</button></div>{confirmDelete&&<div className="delete-confirm"><b>연결된 모든 기록도 함께 삭제됩니다.</b><span>확인을 위해 <strong>{student.name}</strong>을 입력하세요.</span><input value={deleteName} onChange={(event)=>setDeleteName(event.target.value)} placeholder={student.name}/><button type="button" disabled={submitting||deleteName!==student.name} onClick={()=>void remove()}>영구 삭제</button></div>}{error&&<p className="form-error">{error}</p>}<footer><button type="button" className="secondary-button" onClick={onClose}>취소</button><button className="primary" disabled={submitting}>{submitting?"저장 중…":"기본정보 저장"}</button></footer></form><StudentLifecyclePanel supabase={supabase} studentId={student.id} status={values.status} onChanged={(status)=>{setValues((current)=>({...current,status}));onLifecycleUpdated(status);}}/></>}{data&&tab==="classes"&&<ClassesTab classes={data.classes} guardians={data.guardians} onAssign={rosterStudent?()=>onAssign(rosterStudent):undefined}/>} {data&&tab==="attendance"&&<AttendanceTab attendance={data.attendance} makeups={data.makeups}/>} {data&&tab==="learning"&&<LearningTab assignments={data.assignments} consultations={data.consultations}/>}</div></section></div>;
}

function SummaryTab({data}:{data:DetailData}){const rate=data.summary.attendanceTotal?Math.round(data.summary.present/data.summary.attendanceTotal*100):null;const colors=new Map(data.timetable.map((row)=>[row.className,row.color]));return <><div className="student-summary-cards"><article><span>최근 출석률</span><b>{rate===null?"-":`${rate}%`}</b><small>출석 {data.summary.present} · 지각 {data.summary.late} · 결석 {data.summary.absent}</small></article><article><span>미완료 과제</span><b>{data.summary.assignmentOpen}</b><small>제출 전·첨삭 대기</small></article><article><span>예정 보강</span><b>{data.summary.upcomingMakeups}</b><small>앞으로 진행할 일정</small></article><article><span>최근 상담</span><b>{data.summary.lastConsultedAt?formatDate(data.summary.lastConsultedAt):"없음"}</b><small>학생·학부모 상담 기준</small></article></div><section className="student-overview"><div><h3>현재 수강</h3>{data.classes.filter((item)=>item.status==="active").length?data.classes.filter((item)=>item.status==="active").map((item)=><p key={item.id}><i style={{background:colors.get(item.name)??"#922D61"}}/><span><b>{item.name}</b><small>{item.subject} · {item.teachers||"담당 미배정"}</small></span></p>):<Empty text="현재 수강 중인 클래스가 없습니다."/>}</div><div><h3>학부모 연락처</h3>{data.guardians.length?data.guardians.map((item)=><p key={item.id}><span><b>{item.name}{item.isPrimary?" · 주 보호자":""}</b><small>{item.phone} · {item.relationship||"보호자"}</small></span></p>):<Empty text="등록된 학부모가 없습니다."/>}</div></section><section className="student-personal-schedule"><header><h3>개인 수업 시간표</h3><p>현재 수업이 있는 요일과 시간만 표시합니다.</p></header><StudentRelevantTimetable rows={data.timetable}/></section></>}
function ClassesTab({classes,guardians,onAssign}:{classes:ClassItem[];guardians:Guardian[];onAssign?:()=>void}){return <><div className="hub-section-title"><div><h3>수강 클래스</h3><p>현재 및 이전 수강 기록입니다.</p></div>{onAssign&&<button className="primary" onClick={onAssign}>＋ 클래스 배정</button>}</div><div className="student-record-list">{classes.length?classes.map((item)=><article key={item.id}><i className={item.status} style={item.status==="active"?{background:item.color??"#922D61"}:undefined}/><span><b>{item.name}</b><small>{item.subject}{item.room?` · ${item.room}`:""} · {item.teachers||"담당 미배정"}</small></span><em>{item.status==="active"?"수강 중":item.status==="paused"?"일시 중지":"종료"}</em></article>):<Empty text="수강 기록이 없습니다."/>}</div><div className="hub-section-title compact"><div><h3>학부모·보호자</h3></div></div><div className="guardian-grid">{guardians.length?guardians.map((item)=><article key={item.id}><b>{item.name}</b><span>{item.phone}</span><small>{item.relationship||"보호자"}{item.isPrimary?" · 주 보호자":""}</small></article>):<Empty text="등록된 보호자가 없습니다."/>}</div></>}
function AttendanceTab({attendance,makeups}:{attendance:AttendanceItem[];makeups:MakeupItem[]}){return <><div className="hub-section-title"><div><h3>최근 출결</h3><p>최근 30건을 표시합니다.</p></div></div><div className="student-record-list">{attendance.length?attendance.map((item)=><article key={item.id}><time>{formatDate(item.lessonDate)}</time><span><b>{item.className}</b><small>{item.note||"메모 없음"}</small></span><em className={item.status}>{statusLabels[item.status]}</em></article>):<Empty text="입력된 출결 기록이 없습니다."/>}</div><div className="hub-section-title compact"><div><h3>보강 일정</h3></div></div><div className="student-record-list">{makeups.length?makeups.map((item)=><article key={item.id}><time>{formatDateTime(item.scheduledAt)}</time><span><b>{item.className} · {item.teacherName}</b><small>{item.room}{item.note?` · ${item.note}`:""}</small></span><em>{item.status==="scheduled"?"예정":item.status==="completed"?"완료":"취소"}</em></article>):<Empty text="보강 일정이 없습니다."/>}</div></>}
function LearningTab({assignments,consultations}:{assignments:AssignmentItem[];consultations:ConsultationItem[]}){return <><div className="hub-section-title"><div><h3>최근 과제·첨삭</h3><p>최근 과제 20건을 표시합니다.</p></div></div><div className="student-record-list">{assignments.length?assignments.map((item)=><article key={item.id}><time>{formatDate(item.dueAt)} 마감</time><span><b>{item.title}</b><small>{item.className}{item.feedback?` · ${item.feedback}`:""}</small></span><em className={item.status}>{item.status==="reviewed"?"첨삭 완료":item.status==="submitted"?"첨삭 대기":"제출 전"}</em></article>):<Empty text="등록된 과제가 없습니다."/>}</div><div className="hub-section-title compact"><div><h3>최근 상담</h3><p>교직원 내부 기록입니다.</p></div></div><div className="consultation-timeline">{consultations.length?consultations.map((item)=><article key={item.id}><time>{formatDateTime(item.consultedAt)}</time><span><b>{consultationLabels[item.type]||item.type} 상담 · {item.consultantName}</b><p>{item.internalNote||"내부 메모 없음"}</p>{item.nextContactOn&&<small>다음 연락 {formatDate(item.nextContactOn)}</small>}</span></article>):<Empty text="상담 기록이 없습니다."/>}</div></>}
function Empty({text}:{text:string}){return <p className="student-hub-empty">{text}</p>}
function formatDate(value:string){return new Intl.DateTimeFormat("ko-KR",{timeZone:"Asia/Seoul",month:"short",day:"numeric"}).format(new Date(value))}
function formatDateTime(value:string){return new Intl.DateTimeFormat("ko-KR",{timeZone:"Asia/Seoul",month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",hour12:false}).format(new Date(value))}
function studentStatus(value:string){return value==="active"||value==="재원"?"재원":value==="paused"||value==="휴원"?"휴원":"퇴원"}
