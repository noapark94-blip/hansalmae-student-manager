"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import type { SupabaseClient } from "@supabase/supabase-js";
import type { Profile } from "./supabase";

type FamilyView="schedule"|"attendance"|"makeups"|"assignments"|"consultations"|"communications";
type Child={ id:string; name:string; school:string|null; grade:string|null };
type ClassItem={ id:string; name:string; subject:string; room:string|null; color:string; weekday:number; startTime:string; endTime:string; teachers:string };
type Upcoming={ id:string; name:string; subject:string; room:string|null; color:string; classDate:string; startTime:string; teachers:string };
type Assignment={ id:string; title:string; className:string; dueAt:string; status:string; feedback:string|null };
type Attendance={ id:string; lessonDate:string; className:string; status:string; note:string|null };
type Makeup={ id:string; className:string; scheduledAt:string; room:string; status:string; teacherName:string };
type Announcement={ id:string; title:string; body:string; publishedAt:string; authorName:string };
type Consultation={ id:string; consultedAt:string; type:string; consultantName:string; summary:string; nextContactOn:string|null };
type ExamProgressItem={ id:string; lessonDate:string; className:string; subject:string; mainSubject:string; examTitle:string|null; score:number|null; maxScore:number; percent:number|null; evaluation:string|null; feedback:string|null; teacherName:string };
type Data={ role:"student"|"guardian"; children:Child[]; selectedStudent:Child|null; weekClasses:ClassItem[]; upcomingClasses:Upcoming[]; attendanceSummary:{ total:number; present:number; late:number; absent:number }; recentAttendance:Attendance[]; makeups:Makeup[]; assignments:Assignment[]; announcements:Announcement[]; consultations:Consultation[] };
const weekdays=["월","화","수","목","금","토","일"];
const attendanceLabels:Record<string,string>={present:"출석",late:"지각",absent:"결석",excused:"인정"};

export function FamilyLiveDashboard({supabase,profile,onNavigate}:{supabase:SupabaseClient;profile:Profile;onNavigate:(view:FamilyView)=>void}){
  const [data,setData]=useState<Data|null>(null); const [selectedId,setSelectedId]=useState<string|null>(null); const [loading,setLoading]=useState(true); const [error,setError]=useState("");
  const load=useCallback(async(id:string|null)=>{setLoading(true);setError("");const{data:next,error:loadError}=await supabase.rpc("family_live_dashboard",{p_student_id:id});if(loadError)setError("학습 현황을 불러오지 못했습니다.");else{const parsed=next as Data;setData(parsed);setSelectedId(parsed.selectedStudent?.id??null);}setLoading(false);},[supabase]);
  useEffect(()=>{void load(null);},[load]);
  const rate=useMemo(()=>data?.attendanceSummary.total?Math.round(data.attendanceSummary.present/data.attendanceSummary.total*100):null,[data]);
  if(loading&&!data)return <section className="panel hub-message">학습 현황을 불러오는 중이에요…</section>;
  if(error&&!data)return <section className="panel hub-message error">{error}</section>;
  const selected=data?.selectedStudent;
  return <><div className="page-heading family-heading"><div><p className="eyebrow">역할 · {profile.role==="student"?"학생":"학부모"}</p><h1>{profile.role==="student"?`안녕하세요, ${profile.display_name}님`:`${selected?.name??"자녀"} 학습 현황`}</h1><p>{profile.role==="student"?"내 수업과 학습 현황을 확인하세요.":"자녀별 수업과 출결·과제 현황을 확인하세요."}</p></div>{profile.role==="guardian"&&(data?.children.length??0)>1&&<label className="child-selector">자녀 선택<select disabled={loading} value={selectedId??""} onChange={(event)=>void load(event.target.value)}>{data?.children.map((child)=><option key={child.id} value={child.id}>{child.name} · {[child.school,child.grade].filter(Boolean).join(" ")}</option>)}</select></label>}</div>{error&&<p className="attendance-error">{error}</p>}{!selected?<section className="panel family-empty"><b>연결된 학생 정보가 없습니다.</b><span>학원 관리자에게 학생 또는 자녀 계정 연결을 요청해 주세요.</span></section>:<><section className="stats-grid family-stats"><FamilyStat label="주간 수업" value={String(data?.weekClasses.length??0)} unit="개" detail={data?.upcomingClasses[0]?`다음 ${formatUpcoming(data.upcomingClasses[0])}`:"예정 수업 없음"} icon="▦" tone="blue"/><FamilyStat label="이번 달 출석률" value={rate===null?"–":String(rate)} unit={rate===null?"":"%"} detail={`출석 ${data?.attendanceSummary.present??0} · 지각 ${data?.attendanceSummary.late??0} · 결석 ${data?.attendanceSummary.absent??0}`} icon="✓" tone="green"/><FamilyStat label="확인할 과제" value={String(data?.assignments.filter((item)=>item.status!=="reviewed").length??0)} unit="건" detail="제출 전·첨삭 대기" icon="✎" tone="amber"/><FamilyStat label="예정 보강" value={String(data?.makeups.filter((item)=>item.status==="scheduled").length??0)} unit="건" detail="앞으로 진행할 보강" icon="↻" tone="wine"/></section><FamilyExamProgress supabase={supabase} studentId={selected.id}/><div className="family-dashboard-grid"><section className="panel family-upcoming"><header><h2>다가오는 수업</h2><button onClick={()=>onNavigate("schedule")}>주간 시간표 ›</button></header>{data?.upcomingClasses.length?<div>{data.upcomingClasses.map((item)=><article key={`${item.id}-${item.classDate}`}><time>{formatClassDate(item.classDate)}<b>{item.startTime.slice(0,5)}</b></time><i style={{background:item.color}}/><span><b>{item.name}</b><small>{item.teachers||"담당 선생님"}{item.room?` · ${item.room}`:""}</small></span></article>)}</div>:<Empty text="다가오는 수업이 없습니다."/>}</section><section className="panel family-notices"><header><h2>새 공지</h2><button onClick={()=>onNavigate("communications")}>전체 공지 ›</button></header>{data?.announcements.length?<div>{data.announcements.slice(0,4).map((item)=><article key={item.id}><span><b>{item.title}</b><p>{item.body}</p><small>{item.authorName} · {formatDate(item.publishedAt)}</small></span></article>)}</div>:<Empty text="새로운 공지가 없습니다."/>}</section><section className="panel family-week"><header><h2>주간 시간표</h2></header><div>{weekdays.slice(0,6).map((day,index)=><section key={day}><b>{day}</b>{data?.weekClasses.filter((item)=>item.weekday===index+1).map((item)=><span key={item.id} style={{borderColor:item.color}}><strong>{item.startTime.slice(0,5)}</strong><em>{item.name}</em><small>{item.room||item.subject}</small></span>)}</section>)}</div></section><section className="panel family-progress"><header><h2>최근 학습 현황</h2></header><div className="family-progress-list"><button onClick={()=>onNavigate("attendance")}><i className="green">✓</i><span><b>최근 출결</b><small>{data?.recentAttendance[0]?`${formatDate(data.recentAttendance[0].lessonDate)} · ${data.recentAttendance[0].className} · ${attendanceLabels[data.recentAttendance[0].status]}`:"출결 기록 없음"}</small></span><strong>{data?.recentAttendance.length??0}</strong></button><button onClick={()=>onNavigate("makeups")}><i className="wine">↻</i><span><b>보강 일정</b><small>{data?.makeups[0]?`${formatDateTime(data.makeups[0].scheduledAt)} · ${data.makeups[0].className}`:"예정된 보강 없음"}</small></span><strong>{data?.makeups.filter((item)=>item.status==="scheduled").length??0}</strong></button><button onClick={()=>onNavigate("assignments")}><i className="amber">✎</i><span><b>과제·첨삭</b><small>{data?.assignments[0]?`${data.assignments[0].title} · ${assignmentLabel(data.assignments[0].status)}`:"확인할 과제 없음"}</small></span><strong>{data?.assignments.filter((item)=>item.status!=="reviewed").length??0}</strong></button><button onClick={()=>onNavigate("consultations")}><i className="blue">☏</i><span><b>공개 상담 요약</b><small>{data?.consultations[0]?`${formatDate(data.consultations[0].consultedAt)} · ${data.consultations[0].summary}`:"공개된 상담 요약 없음"}</small></span><strong>{data?.consultations.length??0}</strong></button></div></section></div></>}</>;
}

function FamilyExamProgress({supabase,studentId}:{supabase:SupabaseClient;studentId:string}){
  const[items,setItems]=useState<ExamProgressItem[]>([]);const[selectedSubject,setSelectedSubject]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");
  useEffect(()=>{let active=true;setLoading(true);setError("");setSelectedSubject("");void supabase.rpc("family_exam_progress",{p_student_id:studentId}).then(({data,error:loadError})=>{if(!active)return;if(loadError)setError("시험 성적과 피드백을 불러오지 못했습니다.");else setItems((data??[]) as ExamProgressItem[]);setLoading(false);});return()=>{active=false;};},[studentId,supabase]);
  const subjects=useMemo(()=>Array.from(new Set(items.map((item)=>item.subject))),[items]);
  const subject=selectedSubject&&subjects.includes(selectedSubject)?selectedSubject:subjects[0]??"";
  const selectedItems=useMemo(()=>items.filter((item)=>item.subject===subject),[items,subject]);
  const scored=useMemo(()=>selectedItems.filter((item)=>item.percent!==null).slice(0,8).reverse(),[selectedItems]);
  return <section className="panel family-exam-progress"><header><div><p className="eyebrow">과목별 학습 성장</p><h2>시험 성적·선생님 피드백</h2></div>{subjects.length>0&&<nav aria-label="과목 선택">{subjects.map((item)=><button type="button" key={item} className={subject===item?"active":""} onClick={()=>setSelectedSubject(item)}>{item}</button>)}</nav>}</header>{loading?<Empty text="시험 기록을 불러오는 중이에요…"/>:error?<p className="family-exam-error">{error}</p>:!items.length?<Empty text="아직 등록된 시험 결과가 없습니다."/>:<div className="family-exam-body"><ExamTrendChart items={scored}/><div className="family-exam-records">{selectedItems.slice(0,5).map((item)=><article key={item.id}><time>{formatExamDate(item.lessonDate)}</time><span><b>{item.examTitle||`${item.className} 시험`}</b><small>{item.className} · {item.teacherName}</small>{item.evaluation&&<em>{item.evaluation}</em>}{item.feedback&&<p>{item.feedback}</p>}</span><strong>{item.score===null?"평가":formatScore(item.score)}<small>{item.score===null?"점수 미입력":` / ${formatScore(item.maxScore)}점`}</small></strong></article>)}</div></div>}</section>;
}

function ExamTrendChart({items}:{items:ExamProgressItem[]}){
  if(!items.length)return <div className="exam-trend-empty"><b>성적 추이</b><span>점수가 입력된 시험부터 그래프가 표시됩니다.</span></div>;
  const points=items.map((item,index)=>{const x=items.length===1?300:35+index*(530/(items.length-1));const y=150-(item.percent??0)*1.2;return{x,y,item};});
  return <div className="exam-trend"><header><b>최근 성적 추이</b><span>만점 대비 백분율</span></header><svg viewBox="0 0 600 180" role="img" aria-label={`최근 시험 성적 추이: ${items.map((item)=>`${formatExamDate(item.lessonDate)} ${item.percent}%`).join(", ")}`}><line x1="35" y1="30" x2="565" y2="30"/><line x1="35" y1="90" x2="565" y2="90"/><line x1="35" y1="150" x2="565" y2="150"/><text x="4" y="34">100</text><text x="12" y="94">50</text><text x="19" y="154">0</text>{points.length>1&&<polyline points={points.map((point)=>`${point.x},${point.y}`).join(" ")}/>} {points.map((point)=><g key={point.item.id}><circle cx={point.x} cy={point.y} r="6"><title>{formatExamDate(point.item.lessonDate)} · {point.item.percent}%</title></circle><text className="point-value" x={point.x} y={point.y-12} textAnchor="middle">{point.item.percent}%</text></g>)}</svg><div className="exam-trend-dates">{points.map((point)=><span key={point.item.id}>{formatShortExamDate(point.item.lessonDate)}</span>)}</div></div>;
}
function FamilyStat({label,value,unit,detail,icon,tone}:{label:string;value:string;unit:string;detail:string;icon:string;tone:string}){return <article className="stat-card"><span className={`stat-icon ${tone}`}>{icon}</span><div><span>{label}</span><strong>{value}<i>{unit}</i></strong><small>{detail}</small></div></article>}
function Empty({text}:{text:string}){return <p className="family-list-empty">{text}</p>}
function formatDate(value:string){return new Intl.DateTimeFormat("ko-KR",{timeZone:"Asia/Seoul",month:"short",day:"numeric"}).format(new Date(value))}
function formatDateTime(value:string){return new Intl.DateTimeFormat("ko-KR",{timeZone:"Asia/Seoul",month:"short",day:"numeric",hour:"2-digit",minute:"2-digit",hour12:false}).format(new Date(value))}
function formatClassDate(value:string){return new Intl.DateTimeFormat("ko-KR",{timeZone:"Asia/Seoul",weekday:"short",month:"numeric",day:"numeric"}).format(new Date(value))}
function formatUpcoming(item:Upcoming){return `${formatClassDate(item.classDate)} ${item.startTime.slice(0,5)}`}
function assignmentLabel(status:string){return status==="reviewed"?"첨삭 완료":status==="submitted"?"첨삭 대기":"제출 전"}
function formatExamDate(value:string){return new Intl.DateTimeFormat("ko-KR",{timeZone:"Asia/Seoul",year:"numeric",month:"short",day:"numeric"}).format(new Date(`${value}T12:00:00+09:00`))}
function formatShortExamDate(value:string){return new Intl.DateTimeFormat("ko-KR",{timeZone:"Asia/Seoul",month:"numeric",day:"numeric"}).format(new Date(`${value}T12:00:00+09:00`))}
function formatScore(value:number){return Number.isInteger(value)?String(value):value.toFixed(1)}
